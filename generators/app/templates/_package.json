{
  "name": "<%= name %>",
  "version": "1.0.0",
  "scripts": {
    "start": "idyll index.idl --css styles.css",
    "build": "cp -r {images,styles.css} build/; idyll index.idl --build | uglifyjs > build/index.js",
    "deploy": "gh-pages -d ./build"
  },
  "dependencies": {
    "idyll": "^1.0.0"
  },
  "devDependencies": {
    "uglify-js": "^2.7.5",
    "gh-pages": "^0.12.0"
  }
}
