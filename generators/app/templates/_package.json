{
  "name": "<%= name %>",
  "version": "1.0.0",
  "scripts": {
    "start": "idyll index.idl --css styles.css --layout blog --spellcheck",
    "build": "cp -r {.idyll/styles.css,images} build/; idyll index.idl --layout blog --css styles.css --build | uglifyjs > build/bundle.js; cp index.html build/index.html",
    "deploy": "npm run build && gh-pages -d ./build"
  },
  "dependencies": {
    "idyll": "^1.0.0"
  },
  "devDependencies": {
    "uglify-js": "^2.7.5",
    "gh-pages": "^0.12.0"
  }
}
